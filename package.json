{
  "name": "aws-profile-select",
  "version": "0.0.1",
  "description": "Ensure a profile is selected for the current bash session",
  "main": "aws-profile-select",
  "bin": {
    "aws-profile-select": "./aws-profile-select"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nconf": "^0.8.5",
    "select-shell": "^1.0.0",
    "yargs": "^10.0.3"
  }
}
